services:
  mysql:
    image: mysql:8.0
    container_name: event_manager_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: event_manager
      MYSQL_USER: event_manager
      MYSQL_PASSWORD: qwerty
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
